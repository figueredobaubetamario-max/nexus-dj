name: nexus_dj
description: NEXUS DJ App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  just_audio: ^0.9.36
  file_picker: ^6.1.1
  provider: ^6.1.1
  google_fonts: ^6.1.0
  permission_handler: ^11.1.0
  wakelock_plus: ^1.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true